+++
title = "MCP Tool: lattice.provider.list"
description = "List configured providers with compact health and connection state; does not connect them."
[extra]
generated = true
source = "lattice-mcp"
+++

<!-- GENERATED BY lattice-docgen. DO NOT EDIT MANUALLY. -->

# MCP Tool: lattice.provider.list

## Purpose

List configured providers with compact health and connection state; does not connect them.

## Input schema

```json
{
  "additionalProperties": false,
  "type": "object"
}
```

## Output schema

```json
{
  "$defs": {
    "ProviderSummaryResponse": {
      "properties": {
        "connection_state": {
          "type": "string"
        },
        "health": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "provider_id": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "tool_count": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "provider_id",
        "name",
        "kind",
        "connection_state",
        "health",
        "reason",
        "tool_count"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "providers": {
      "items": {
        "$ref": "#/$defs/ProviderSummaryResponse"
      },
      "type": "array"
    }
  },
  "required": [
    "providers"
  ],
  "title": "ProviderListResponse",
  "type": "object"
}
```

## Safety

This northbound operation is subject to Lattice policy and records an operation-level audit entry. Provider tools are discovered separately through progressive tool search.
