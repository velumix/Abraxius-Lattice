+++
title = "MCP Tool: lattice.search"
description = "Search the canonical Roblox workspace and return compact stable rbx:// references and evidence IDs."
[extra]
generated = true
source = "lattice-mcp"
+++

<!-- GENERATED BY lattice-docgen. DO NOT EDIT MANUALLY. -->

# MCP Tool: lattice.search

## Purpose

Search the canonical Roblox workspace and return compact stable rbx:// references and evidence IDs.

## Input schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "limit": {
      "description": "Maximum inline hits; values are bounded to 1..50",
      "format": "uint",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "query": {
      "description": "Exact name, symbol, path, or source terms to find",
      "type": "string"
    }
  },
  "required": [
    "query"
  ],
  "title": "SearchRequest",
  "type": "object"
}
```

## Output schema

```json
{
  "$defs": {
    "SearchHitResponse": {
      "properties": {
        "content_hash": {
          "type": "string"
        },
        "display_path": {
          "type": "string"
        },
        "evidence_id": {
          "type": "string"
        },
        "evidence_origin": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "resource_ref": {
          "type": "string"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "score_milli": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "resource_ref",
        "display_path",
        "name",
        "score_milli",
        "content_hash",
        "evidence_id",
        "evidence_origin",
        "revision"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "hits": {
      "items": {
        "$ref": "#/$defs/SearchHitResponse"
      },
      "type": "array"
    },
    "query": {
      "type": "string"
    },
    "truncated": {
      "type": "boolean"
    }
  },
  "required": [
    "query",
    "hits",
    "truncated"
  ],
  "title": "SearchResponse",
  "type": "object"
}
```

## Safety

This northbound operation is subject to Lattice policy and records an operation-level audit entry. Provider tools are discovered separately through progressive tool search.
